(function (window) {
  window.env = window.env || {};

  // ============================================
  // Enterprise Verifier Proximity PWA
  // Runtime Configuration (generated by nginx entrypoint)
  // ============================================
  
  // ── Backend Configuration ──
  // PWA consumes eudistack-core-verifier backend via HTTP + SSE
  // When served via nginx, use empty string (same origin - nginx proxies /api/ and /oid4vp/)
  window["env"]["verifierBackendUrl"] = "${VERIFIER_BACKEND_URL}";
  
  // ── SSE Configuration ──
  window["env"]["sseTimeout"] = 120000; // 120 seconds
  
  // ── QR Code Configuration ──
  window["env"]["qrExpirationSeconds"] = 120; // 2 minutes
  
  // ── Multi-Tenancy Configuration ──
  // Tenant is injected by nginx entrypoint from TENANT env var
  window["env"]["tenant"] = "${TENANT_DEFAULT}";

  // ── Wallet URL ──
  // Used to build scannable HTTPS QR codes (camera-friendly) instead of openid4vp://
  window["env"]["walletUrl"] = "${WALLET_URL}";
  
  // ── Legacy Configuration (Deprecated) ──
  window["env"]["logs_enabled"] = "${LOGS_ENABLED}";
  window["env"]["verifier_mode"] = "browser";
})(this);
